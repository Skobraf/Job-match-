import React from 'react';
import { connect } from 'react-redux';

const List = (props) => {
    return(
        <div>
            <p>
                hello world
            </p>
        </div>
    );
}
export default List;